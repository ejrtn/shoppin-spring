<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.shopping.user.UserMapper">


    <insert id="insertMember">
        INSERT INTO member (member_Id, username, age)
        VALUES (#{member_Id}, #{member.username}, #{member.age})
    </insert>

<!--    &lt;!&ndash;  Member select 쿼리&ndash;&gt;-->
<!--    <select id="selectMember" resultType="com.gunsproject.guns.study.dto.MemberDto">-->
<!--        SELECT * FROM member-->
<!--        WHERE member_id = #{memberId}-->
<!--    </select>-->

</mapper>